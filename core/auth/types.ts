/**
 * Описания, относящиеся к доступу.
 *
 * Вынесены отдельно от session.ts: тот работает с куками и базой, поэтому
 * исполняется только на сервере. Сами же описания нужны обеим сторонам,
 * и держать их в серверном файле нельзя — иначе браузерная часть потянет
 * за собой серверную.
 */

/** Вошедший в систему пользователь вместе с его правами. */
export interface CurrentUser {
  id: string;
  programId: string;
  email: string;
  name: string;
  roles: { key: string; name: string }[];
  permissions: string[];
}
